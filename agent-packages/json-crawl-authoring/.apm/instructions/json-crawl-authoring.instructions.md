---
description: Conventions for extending the json-crawl traversal library.
applyTo: "src/**/*.ts"
---

## Skill trigger: `json-crawl-authoring`

When editing the json-crawl library under `src/` — changing crawl/clone
behaviour, hook lifecycle, path rules, or exported utilities — apply the
`json-crawl-authoring` skill.
